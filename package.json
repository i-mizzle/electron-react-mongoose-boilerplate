{
  "name": "electron-react-mongoose",
  "version": "1.0.0",
  "description": "",
  "main": "electron.js",
  "scripts": {
    "start": "concurrently \"npm run start-electron\" \"npm run start-react\"",
    "start-electron": "electron .",
    "start-react": "react-scripts start",
    "build": "react-scripts build && electron-builder",
    "build:windows": "react-scripts build && electron-builder --win",
    "build:mac": "react-scripts build && electron-builder --mac",
    "build:linux": "react-scripts build && electron-builder --linux"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "electron": "^25.2.0",
    "electron-devtools-installer": "^3.2.0",
    "mongoose": "^7.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "electron-builder": "^22.14.1",
    "react-scripts": "^5.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
